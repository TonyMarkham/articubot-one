controller_manager:
  ros__parameters:
    update_rate: 30
    use_sim_time: true
    
    diff_cont:
      type: diff_drive_controller/DiffDriveController
    
    joint_broad:
      type: joint_state_broadcaster/JointStateBroadcaster

diff_cont:
  ros__parameters:
    publish_rate: 50.0

    base_frame_id: base_link

    left_wheel_names: ['left_wheel_joint']
    right_wheel_names: ['right_wheel_joint']
    wheel_separation: 0.35
    wheel_radius: 0.05

    use_stamped_vel: false

    # open_loop: false

    # wheels_per_side: x
    # wheel_separation_multiplier: x
    # left_wheel_radius_multiplier: x
    # right_wheel_radius_multiplier: x

    # odom_frame_id: x
    # pose_covariance_diagonal: x
    # twist_covariance_diagonal: x
    # open_loop: x
    # enable_odom_tf: x

    # cmd_vel_timeout: x
    # publish_limited_velocity: x
    # velocity_rolling_window_size: x

    # linear.x.has_velocity_limits: false
    # linear.x.has_acceleration_limits: false
    # linear.x.has_jerk_limits: false
    # linear.x.max_velocity_limits: NAN
    # linear.x.min_velocity_limits: NAN
    # linear.x.max_acceleration_limits: NAN
    # linear.x.min_acceleration_limits: NAN
    # linear.x.max_jerk_limits: NAN
    # linear.x.min_jerk_limits: NAN

    # linear.z.has_velocity_limits: false
    # linear.z.has_acceleration_limits: false
    # linear.z.has_jerk_limits: false
    # linear.z.max_velocity_limits: NAN
    # linear.z.min_velocity_limits: NAN
    # linear.z.max_acceleration_limits: NAN
    # linear.z.min_acceleration_limits: NAN
    # linear.z.max_jerk_limits: NAN
    # linear.z.min_jerk_limits: NAN

# diff_cont:
  # ros__parameters: